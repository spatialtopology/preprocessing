[codespell]
skip = .git,venvs,*.ipynb,*.csv,*.acq,*.e,*.pyc,*.o,*.tsv
# te -- check if should be fixed or not or ignored
# preemptable -- could be preemptible which is more popular but I am not a judge here
ignore-words-list = te,preemptable
